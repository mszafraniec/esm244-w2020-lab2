#attach packages

library(tidyverse)
library(shiny)
library(shinythemes)

#create user interface

ui<-fluidPage()

server<-function(input,output){} #function tells it what factors it should be looking at, input from ui, output what goes back to ui
